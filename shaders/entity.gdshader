shader_type canvas_item;

// shader is just a small program runnin on your GPU
// it controls how the pixels look on the screen
// GLSL (OpenGL Shading Language)

void fragment() {
	// Called for every pixel the material is visible on.
}
